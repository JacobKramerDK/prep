# Google Calendar OAuth Configuration
# 
# OPTIONAL: Override bundled OAuth credentials with your own
# 
# The app comes with working Google OAuth credentials bundled for convenience.
# If you want to use your own Google Cloud Project credentials instead:
# 
# 1. Create a Google Cloud Project at https://console.cloud.google.com/
# 2. Enable the Google Calendar API
# 3. Create OAuth 2.0 credentials (Desktop application type)
# 4. Add http://localhost:8080/oauth/callback as an authorized redirect URI
# 5. Uncomment and set the values below
# 6. Place this file as .env in the same directory as the Prep executable
#
# GOOGLE_CLIENT_ID=your-client-id-here.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=your-client-secret-here

# OpenAI API Key (required for transcription and brief generation)
OPENAI_API_KEY=sk-your-openai-api-key-here
